{
  "name": "karabiner",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsx src/index.ts",
    "watch": "nodemon --watch src --ext ts --exec \"tsx src/index.ts\"",
    "check": "tsc --noEmit",
    "deploy": "./bin/deploy"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "nodemon": "^3.1.0",
    "tsx": "^4.20.0",
    "typescript": "^5.8.0"
  }
}
